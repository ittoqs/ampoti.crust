[package]
name = "ampoti-file"
version = "0.1.0"
edition = "2021"

[dependencies]
iced = { version = "0.12", features = ["tokio"] }
libc = "0.2"
tokio = { version = "1.0", features = ["rt-multi-thread"] }

[build-dependencies]
cc = "1.0"
